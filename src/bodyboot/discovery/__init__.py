"""Discovery: describe an unfamiliar SDK without deciding what anything means.

Import the submodules directly (``bodyboot.discovery.scanner`` etc.); this
package initialiser stays empty to keep the safety policy free of import cycles.
"""
