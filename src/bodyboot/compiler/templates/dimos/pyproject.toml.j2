# GENERATED by BODYBOOT {{ generator_version }} (run {{ run_id }})
[project]
name = "{{ dist_name }}"
version = "0.1.0"
description = "BODYBOOT-generated external dimOS integration for the {{ plan.sdk_package }} vendor SDK (motion mock-only)"
requires-python = ">=3.10,<3.13"
dependencies = [
    "dimos>=0.0.14",
    "numpy",
]

[project.entry-points."dimos.blueprints"]
robot = "bodyboot_generated_robot.blueprints:bodyboot_robot"
connection = "bodyboot_generated_robot.connection_module:BodybootConnection"
skills = "bodyboot_generated_robot.skill_container:BodybootSkillContainer"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
