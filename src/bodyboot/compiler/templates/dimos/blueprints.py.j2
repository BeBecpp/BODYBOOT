"""GENERATED by BODYBOOT {{ generator_version }} (run {{ run_id }}) - dimOS blueprints.

Exposed to dimOS through the ``dimos.blueprints`` entry-point group declared in
pyproject.toml. dimos/robot/all_blueprints.py is never touched.

    dimos run {{ dist_name }}.robot
"""

from dimos.core.coordination.blueprints import autoconnect

from .connection_module import BodybootConnection
from .skill_container import BodybootSkillContainer

bodyboot_robot = autoconnect(
    BodybootConnection.blueprint(),
    BodybootSkillContainer.blueprint(),
)
